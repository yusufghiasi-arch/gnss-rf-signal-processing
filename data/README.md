# Data Folder

Large raw `.bin` files are not stored in this GitHub repository.

To run the raw IF/IQ examples, place local binary files here manually.

Expected example filenames:

```text
sample_if_data_1.bin
sample_if_data_2.bin
